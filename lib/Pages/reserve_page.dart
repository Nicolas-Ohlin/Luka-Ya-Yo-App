import 'package:flutter/material.dart';

class Reserve extends StatelessWidget {
  const Reserve({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(
        child: Text(
          'hello page des maisons reservées',
        ),
      ),
    );
  }
}
